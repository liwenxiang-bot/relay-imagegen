# 环境变量方式配置（优先级高于 config.json）
# 复制成 .env 后填写，或直接在 shell 里 export

OPENAI_API_KEY=sk-你的中转站key
OPENAI_BASE_URL=https://your-relay.com/v1
OPENAI_IMAGE_MODEL=gpt-image-2

# 也支持这些别名，用于和其他工具共存时避免冲突：
# RELAY_IMAGE_API_KEY / RELAY_IMAGE_BASE_URL / RELAY_IMAGE_MODEL
# IMG_API_KEY / IMG_BASE_URL / IMG_MODEL
